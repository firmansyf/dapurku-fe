export interface ErrorResponseData {
    error?: string
    message?: string
    status?: number
  }
  
  export interface ErrorResponse {
    error: string
    message: string
    status: number
  }
  